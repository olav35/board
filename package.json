{
  "name": "board",
  "version": "1.0.0",
  "description": "A shitty board",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "env $(cat .env.dev | grep -v \"#\" | xargs) nodemon server.js",
    "cypress:open": "cypress open",
    "start:test": "env $(cat .env.test | grep -v \"#\" | xargs) nodemon server.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/olav35/board.git"
  },
  "author": "Olav Fosse",
  "license": "UNLICENSED",
  "bugs": {
    "url": "https://github.com/olav35/board/issues"
  },
  "homepage": "https://github.com/olav35/board#readme",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "cypress": "^4.12.0",
    "nodemon": "^2.0.4"
  }
}
